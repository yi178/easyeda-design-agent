# 工程1：原理图读取、写回与闭环还原计划

## 目标

工程1的核心目标是先完成第一个可验证能力：

```text
读取原理图 -> 形成统一 Snapshot / Graph -> 自动生成可导入原理图 -> 再读取对比 -> 验证是否能还原
```

这不是单纯把 JSON 读出来，而是要证明系统能理解原理图结构，并能在后续语义生成阶段复用同一套中间表示。

## 边界

当前阶段默认只读和离线写回：

- 不直接修改用户当前打开的立创 EDA 工程。
- 不添加任意 JavaScript 执行入口。
- 不添加下单、支付、制造相关自动流程。
- 插件侧继续以只读导出为主。
- 写操作先生成离线 EasyEDA Standard JSON 或 `DesignPlan`，不直接写入编辑器。
- 后续真正写入立创 EDA 必须经过 `DesignPlan -> dry-run -> human approval -> validation -> apply/reject`。

## 总体路线

### 阶段 1：离线读取 EasyEDA / LCEDA Standard JSON

先处理已经下载到本地的公开工程：

```text
runs/third-party/easyeda-samples/
```

当前样例：

| 工程 | 原理图规模 | 用途 |
|---|---:|---|
| OpenSpool Mini daughterboard | 60 shapes | 第一优先级，小而真实 |
| KUSBA | 229 shapes | 中等复杂度，适合发现 labels / ports / wires 问题 |
| STM32-FOC simplefoc103 | 356 shapes | MCU + CAN + 电机驱动，适合后续语义验证 |

输入：

```text
EasyEDA Standard SCH.json
```

输出：

```text
SchematicSnapshot
SchematicGraph
raw shape sidecar
```

需要识别的对象：

- `LIB`：元件符号
- `W`：导线
- `N`：网络标签
- `F`：电源符号、端口、网络标识等
- `J`：连接点
- `O`：非连接、图形或其他对象
- `T`：文本
- `R`：矩形或图形对象

第一阶段验收标准：

- 能读取 `schematics[].dataStr.shape`。
- 能输出统一 `SchematicSnapshot`。
- 组件数量、主要 `ref`、pin 数量大体正确。
- wire points 非空。
- labels / ports / power flags 不再混成 `U?` 伪组件。
- 能构建 `SchematicGraph`。

建议脚本：

```text
scripts/easyeda-std-to-snapshot.mjs
```

建议命令：

```powershell
node scripts\easyeda-std-to-snapshot.mjs `
  runs\third-party\easyeda-samples\openspool\hardware\openspool-mini-daughterboard\v1.1\SCH_OpenSpool-Mini-Daughterboard_2025-06-16.json `
  runs\roundtrip\openspool-daughterboard\snapshot.json
```

## 阶段 2：离线写回 EasyEDA Standard JSON

第二阶段不是直接写立创 EDA 当前工程，而是生成新的可导入原理图文件：

```text
snapshot.json -> SCH.roundtrip.json
```

初期策略要保守：

- 元件符号优先保留原始 `LIB` shape。
- 导线、标签、端口、电源符号从 IR 重新生成。
- 布局坐标先保留原始坐标。
- 不追求美化排版，先追求可打开、拓扑接近、语义不丢。

输出：

```text
runs/roundtrip/<project>/SCH.roundtrip.json
```

建议脚本：

```text
scripts/snapshot-to-easyeda-std.mjs
```

建议命令：

```powershell
node scripts\snapshot-to-easyeda-std.mjs `
  runs\roundtrip\openspool-daughterboard\snapshot.json `
  runs\roundtrip\openspool-daughterboard\SCH.roundtrip.json
```

第二阶段验收标准：

- `SCH.roundtrip.json` 可以被 JSON parser 正常解析。
- 可以导入 EasyEDA / 立创 EDA Standard 或 Pro 的兼容入口。
- 页面能打开，不出现明显空白或崩溃。
- 主要元件、导线、标签可见。
- 原始工程和生成工程的 graph diff 可解释。

## 阶段 3：闭环验证

闭环流程：

```text
原始 SCH.json
  -> snapshot A
  -> graph A

生成 SCH.roundtrip.json
  -> snapshot B
  -> graph B

graph A vs graph B
```

需要比较：

- component refs 是否一致。
- pin 数量是否一致。
- labels / ports / power flags 是否一致。
- wire 数量和端点是否接近。
- critical nets 是否一致。
- graph fingerprint 是否稳定。

关键网络示例：

```text
GND
+3V3
VCC
NRST
BOOT0
SWDIO
SWCLK
CANH
CANL
USB_D+
USB_D-
FB
SW
```

建议脚本：

```text
scripts/diff-schematic-graph.mjs
```

建议输出：

```text
runs/roundtrip/<project>/diff-report.json
runs/roundtrip/<project>/diff-report.md
```

闭环验收标准：

- OpenSpool daughterboard 首先通过基本闭环。
- KUSBA 通过后说明中等复杂度可行。
- STM32-FOC 通过后说明 MCU + CAN + 电机驱动类工程具备继续做语义验证的基础。

## 阶段 4：真实立创 EDA 插件读取

离线闭环稳定后，再处理真实编辑器中的 Pro 工程。

当前插件包：

```text
E:\eda-project\apps\easyeda-bridge-extension\build\dist\easyeda-design-agent-bridge_v0.1.4.eext
```

在立创 EDA 中测试顺序：

```text
EasyEDA Design Agent -> Show Status / Current Context
EasyEDA Design Agent -> Export Snapshot Summary
EasyEDA Design Agent -> Export Active Schematic Snapshot
EasyEDA Design Agent -> Export Readback Debug JSON
```

需要导出的文件：

```text
fixtures/schematic/captured/<project>-snapshot.json
fixtures/schematic/captured/<project>-readback-debug.json
```

插件读取当前已知问题：

- 部分 labels / ports / power flags 可能被读成一针 `U?` 伪组件。
- wire 对象能读到，但 points 可能为空。
- Pro API 的对象类型和 Standard JSON shape 类型需要建立映射。

`Export Readback Debug JSON` 的作用：

- 列出 EasyEDA 原始对象的 own keys。
- 列出可用 getter 名。
- 固定 allow-list 读取 getter 值。
- 找出一针 `U?` 对象的真实类型线索。
- 找出 wire points 的真实 getter 或字段。

## 阶段 5：真实工程测试顺序

### 离线 JSON 工程

优先顺序：

1. OpenSpool Mini daughterboard
2. KUSBA
3. STM32-FOC simplefoc103
4. SimpleFOCMini
5. RejsaCAN ESP32
6. Open Gamma Detector
7. A4091 hardware

### 立创 / OSHWHub 真实工程

优先顺序：

1. HelloWord Keyboard
2. OpenT12 soldering station controller
3. STM32-FOC motor driver board
4. OV-Watch
5. ESP32S3-SI4732 receiver
6. YuzukiLOHCC PRO HDMI capture card
7. OpenSTM

真实工程测试重点：

- 是否能从立创 EDA 打开工程。
- 插件菜单是否在正确上下文显示。
- status 是否显示 active schematic。
- snapshot 是否有非零 components / pins / wires。
- debug JSON 是否能暴露 labels / ports / power flags / wire points。
- 导出的 snapshot 是否能被本仓库测试脚本读取。

## 阶段 6：插件写入设计

插件写入不能直接做。必须先设计 typed `DesignPlan`。

未来流程：

```text
用户意图 / roundtrip graph
  -> DesignPlan
  -> dry-run preview
  -> deterministic validation
  -> human approval
  -> apply to EDA
  -> readback verify
  -> accept / reject / rollback
```

`DesignPlan` 至少要描述：

- 新增元件
- 删除元件
- 移动元件
- 新增 wire
- 新增 label / port / power flag
- 修改属性
- 预期 net topology
- 验证规则
- 回滚信息

写入验收前置条件：

- 离线读取稳定。
- 离线写回稳定。
- roundtrip diff 可解释。
- 至少一个真实 OSHWHub 工程读取得到稳定 snapshot。
- 至少一个 debug JSON 问题被修复并转成回归测试。

## 工程1的最小可交付版本

最小可交付版本不包含插件写入，只包含离线闭环：

```text
EasyEDA Standard SCH.json
  -> SchematicSnapshot
  -> SchematicGraph
  -> SCH.roundtrip.json
  -> SchematicSnapshot
  -> Graph diff report
```

必须交付：

- `scripts/easyeda-std-to-snapshot.mjs`
- `scripts/snapshot-to-easyeda-std.mjs`
- `scripts/diff-schematic-graph.mjs`
- OpenSpool daughterboard roundtrip 结果
- KUSBA roundtrip 结果
- STM32-FOC roundtrip 初步结果
- 文档化的已知差异和下一步修复项

## 判断是否成功

成功不是“能生成一个 JSON 文件”，而是满足以下条件：

- 人能在 EDA 中打开生成的原理图。
- 程序能读取生成的原理图。
- 原始 graph 和生成 graph 的主要电气拓扑一致。
- 差异报告能清楚说明哪些对象未支持。
- 后续语义生成可以复用同一套 IR，而不是重新写一套生成逻辑。

## 当前下一步

建议立即执行：

1. 实现 `scripts/easyeda-std-to-snapshot.mjs`。
2. 先跑 OpenSpool daughterboard。
3. 修正 `LIB/W/N/F/J/O/T/R` shape 解析。
4. 输出 snapshot 和对象统计报告。
5. 实现最小 `snapshot-to-easyeda-std.mjs`。
6. 生成 `SCH.roundtrip.json`。
7. 做 graph diff。
8. 人工导入 EasyEDA / 立创 EDA 检查视觉还原。

